package net.vukrosic.custommobswordsmod.util.manhunt;

import net.minecraft.client.MinecraftClient;
import net.minecraft.client.gui.hud.InGameHud;
import net.minecraft.client.network.ClientPlayerEntity;
import net.minecraft.client.util.math.MatrixStack;
import net.minecraft.entity.player.PlayerEntity;
import net.minecraft.text.Text;
import net.minecraft.util.math.MathHelper;
import net.vukrosic.custommobswordsmod.command.SetHunterCommand;

public class PreyHealthbarHudManager {

    public static float preyHealth = 20;

            // make these into variables

/*
    public static void setRenderHealthBar(MatrixStack matrices, PlayerEntity player, int x, int y, int lines, int regeneratingHeartIndex, float maxHealth, int lastHealth, int health, int absorption, boolean blinking) {
        SetHunterCommand.pray.sendMessage(Text.of("PreyHealthbarHudManager setRenderHealthBar " + health_saved), false);
        matrices_saved = matrices;
        player_saved = player;
        x_saved = x;
        y_saved = y;
        lines_saved = lines;
        regeneratingHeartIndex_saved = regeneratingHeartIndex;
        maxHealth_saved = maxHealth;
        lastHealth_saved = lastHealth;
        health_saved = health;
        absorption_saved = absorption;
        blinking_saved = blinking;
    }*/
}
